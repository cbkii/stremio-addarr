# ${TAG}

## Install / Upgrade (canonical docs)

Use the primary guides for complete and current steps:

- Install/upgrade guide: [README.md](https://github.com/${REPO}/blob/${TAG}/README.md)
- Hosting/TLS (Caddy + DuckDNS): [README_HOST.md](https://github.com/${REPO}/blob/${TAG}/README_HOST.md)

Release assets:
- [`stremio-addarr-install.tar.gz`](https://github.com/${REPO}/releases/download/${TAG}/stremio-addarr-install.tar.gz)
- [`stremio-addarr-install.tar.gz.sha256`](https://github.com/${REPO}/releases/download/${TAG}/stremio-addarr-install.tar.gz.sha256)

Quick download:

```bash
gh auth login
gh release download --repo ${REPO} --tag ${TAG} --pattern 'stremio-addarr-install.tar.gz*'
```

---

## Version notes

- Add version-specific changes here.
- If config keys changed, list required `.env` updates here.
- If service account expectations changed, state required `User=` / `Group=` updates here.

---

## Minimal verification checklist

After install or upgrade, verify in this order:

- [ ] `sudo systemctl is-active stremio-addarr` returns `active`
- [ ] `curl -fsS http://127.0.0.1:7010/manifest.json >/dev/null` succeeds
- [ ] `curl -fI https://YOUR_HOSTNAME/manifest.json` returns `200`
- [ ] `PUBLIC_BASE_URL` exactly matches the Caddy hostname (`https://YOUR_HOSTNAME`)
- [ ] Stremio installs from `https://YOUR_HOSTNAME/manifest.json`
