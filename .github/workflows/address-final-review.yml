name: Validate final review fixes

on:
  push:
    branches:
      - agent/release-hardening-v1.6.0
    paths:
      - scripts/address-final-review.py
      - scripts/address-review-test-followup.py
      - .github/workflows/address-final-review.yml
  pull_request:
    branches:
      - main
    paths:
      - scripts/address-final-review.py
      - scripts/address-review-test-followup.py
      - .github/workflows/address-final-review.yml

permissions:
  contents: write

jobs:
  validate-and-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/release-hardening-v1.6.0
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - name: Apply fixes
        run: |
          python scripts/address-final-review.py
          python scripts/address-review-test-followup.py
      - name: Install
        run: npm ci
      - name: Validate
        run: |
          set -o pipefail
          git diff --check
          npm run ci 2>&1 | tee final-review-ci.log
          npm run package:release 2>&1 | tee final-review-package.log
          rm -rf release
      - name: Upload diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: final-review-fix-diagnostics
          path: |
            final-review-ci.log
            final-review-package.log
            src/config-ui-core.ts
            assets/configure.js
            test/config-ui-hardening.test.ts
            test/config-ui.test.ts
          if-no-files-found: warn
      - name: Commit validated fixes
        run: |
          rm -f final-review-ci.log final-review-package.log
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/config-ui-core.ts assets/configure.js test/config-ui-hardening.test.ts test/config-ui.test.ts
          git commit -m "harden configuration probes and control routes"
          git push origin HEAD:agent/release-hardening-v1.6.0
